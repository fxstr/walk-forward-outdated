/**
* Executes orders when their conditions are met. 
* @param {array} orders								Orders created by user's algorithms
* @param {TransformableDataSeries} instruments		Instrument that emitted an 'open' event
* @param {DataSeries} account						Account with values for cash, instruments etc.
* @param {Map} positions							Positions held by the user
*/
export default function executeOrder(orders, instrument, account, positions) {

}